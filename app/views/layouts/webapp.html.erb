<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no, minimal-ui">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black">

  <title>DigitalLicence</title>

  <%= stylesheet_link_tag    'webapp', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <body>
    <!-- Status bar overlay for fullscreen mode-->
    <div class="statusbar-overlay"></div>

    <!-- Views-->
    <div class="views">
      <!-- Your main view, should have "view-main" class-->
      <div class="view view-main">
        <!-- Top Navbar-->
        <div class="navbar">
          <div class="navbar-inner">
						<% if !@back_to.blank? %>
							<div class="left">
		            <a href="<%= @back_to %>" class="link">
		            	<i class="icon icon-back"></i>
		            	<span>Back</span>
		            </a>
				      </div>
						<% end %>
            <div class="center sliding">HTML Licence Demo</div>
          </div>
        </div>
        <!-- Pages, because we need fixed-through navbar and toolbar, it has additional appropriate classes-->
        <div class="pages navbar-through">
          <!-- Page, data-page contains page name-->
          <div data-page="index" class="page">
            <!-- Scrollable page content-->
            <div class="page-content">

              <%= yield %>

            </div>
          </div>
        </div>
      </div>
    </div>

    <%= javascript_include_tag 'webapp', 'data-turbolinks-track' => true %>
  </body>

</html>
